1. kiểm tra các action ở app, kiểm tra luồn của accept ảnh hóa đơn ở retrain, và ở các phần modal, các luồn retrain cần kiểm tra và sửa chưa
2. thay hình 3.4 thành nhập liệu ở camera
3. thêm ảnh đợi quét hóa đơn 3.5, 
4. báo cáo xu hương tiếng kiệm lỗi và so sánh peer của user, thêm simulateuser cho các giao dịch mới tới này 20/8 để tôi có thể demo, biểu đồ thu chi cũng sai
5. làm tròn cho gợi ý
6. màu sắc khi tạo ví là sao
7. mục tiêu không liên kết vào ví (ở chi tiết đang còn hiển thị liên kết với ví)
8. thành viên có thể rời nhóm, sửa
10. ẩn tài khảon admin khỏi qunar lí. 
11. [x] gộp trang fusion ai lại với monetization (nợ)
12. tạo tool để sử dụng 1 tài khoản để thử autoban bằng request
13. nếu đã nhập lí do khiểu nại rồi thfi không cần nhập lần 2
14. chuổi ghi chép đnag bị sai
15. miss thông tin từ input người dùng và compare, ràng buộc cho các trường của action type, viết thành 1 file khác để LLM đọc và làm theo prompt hiện tại khác là khó
19. val nếu bill thiếu thông tin ví dụ như số tiền, hoặc thiếu cái khác
16. kiểm tra val cho tất cả input từ phía người dùng, backend và frontend, ở của text và số tiền làm tròn,
17. thanh tiến trình của quá trình train
20/ kiểm thử retrain đối với 3 mô hình
21. kiểm tra đầu ra của AI trước khi đưa ra cho người dùng
22. mô hình mới train được áp dụng thủ công hay tự động(điều kiện của tự độngh) có cần kiểm tra trước khi áp dụng không 
23. LLM Sampling Parameters và Mimo Alert Logic Calibration
24. ở canva không có phóng to ảnh(thêm chức năng)
25. reponsive cho webadmin
#### Sửa luận #####
- thiếu hình 3.4 thành nhập liệu ở camera
- thêm ảnh đợi quét hóa đơn 3.5, 
- thiếu ảnh so sánh 
- nhắc đến phụ lục ở trong đoạn văn và không dùng footnote
- sửa bố cục luận văn chương số 4: test case mới đúng
- in đậm mục lục
- chưa thấy chức năng nào nhắc nói tới RAG
- sơ đồ tổng usecase tổng quát, chưa liên kết với nhau và chưa chi tiết, sửa luôn ERD
- sửa từ ngữ (Bổ trợ tri thức	RAG	Kiến trúc giúp AI trả lời dựa trên thông tin được truy xuất từ cơ sở dữ liệu.)
- giải thích cho kết quả đã đánh giá

#### viết kịch bản demo
